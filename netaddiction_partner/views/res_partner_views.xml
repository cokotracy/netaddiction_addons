<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_netaddiction_partner_form" model="ir.ui.view">
        <field name="name">netaddiction.res.partner.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <!-- Add back customer flag -->
            <xpath expr="//page[@name='sales_purchases']//group[@name='sale']" position="inside">
                <field name="customer"/>
            </xpath>
            <!-- Add back supplier flag -->
            <xpath expr="//page[@name='sales_purchases']//group[@name='purchase']" position="inside">
                <field name="supplier"/>
            </xpath>
        </field>
    </record>
</odoo>
